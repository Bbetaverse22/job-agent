# ATS watchlist. One "ats:company-slug" per line.
#
# The slug is the last path segment of the company's public board URL:
#   boards.greenhouse.io/<slug>   jobs.lever.co/<slug>   jobs.ashbyhq.com/<slug>
#
# Replace this list with the companies you actually want to track. What ships
# here is a starting set of remote-friendly engineering boards, not a
# recommendation. Verify a slug by loading its board URL, or just add it and run
# `python main.py discover`: a bad slug prints one warning and the run continues.
#
# Only postings matching the discover keywords are kept, so a large board costs
# little. See --keywords in the README to widen or narrow that filter.
#
# For your own list, copy this file to companies.local.txt and edit that. It is
# gitignored and, when present, is used instead of this file.

# --- Greenhouse ---
greenhouse:gitlab
greenhouse:grafanalabs
greenhouse:tailscale
greenhouse:chainguard
greenhouse:sourcegraph91
greenhouse:remotecom
greenhouse:vercel

# --- Ashby ---
ashby:supabase
ashby:openai
ashby:langchain
ashby:baseten
ashby:deepgram
ashby:clickhouse
ashby:temporal

# --- Lever ---
# No Lever boards in the default set. Add your own here:
# lever:<slug>

# Boards worth knowing about, but excluded because their listings were onsite
# or hybrid only when this list was last checked. Uncomment if that changes:
# ashby:modal            in-office (NY / SF / Stockholm / London)
# ashby:replit           Foster City hybrid
